{
  "name": "lambda-pack",
  "version": "1.0.0",
  "description": "Small utility to pack node.js source files into a zip to upload it to the lambda function.",
  "author": "Maximilian Schelbach <maximilian@schelbach.de>",
  "bin": {
    "lambda-packer": "index.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc",
    "build": "npm run clean && npm run compile"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/archiver": "^3.1.0",
    "@types/glob": "^7.1.1",
    "@types/node": "^13.9.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "archiver": "^3.1.1",
    "glob": "^7.1.6"
  }
}
